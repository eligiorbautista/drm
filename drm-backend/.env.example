# Environment
NODE_ENV=production
PORT=8000


# Database Configuration (Neon PostgreSQL)
# Neon PostgreSQL Connection URL
# Get your connection string from the Neon dashboard
# Format: postgresql://user:password@hostname/database?sslmode=require
DATABASE_URL=postgresql://neondb_owner:npg_hVXDL7kH8drU@ep-autumn-sea-a1qy7hd0-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Prisma Schema (for local development with SQLite, use DATABASE_SQLITE_URL)
# DATABASE_SQLITE_URL="file:./dev.db"



# CastLabs DRMtoday Configuration
# Merchant ID (UUID) from the DRMtoday dashboard — REQUIRED
DRMTODAY_MERCHANT=f43be426-727f-46ff-ba48-97a208ff40e0
DRMTODAY_ENVIRONMENT=staging

# DRM Key Configuration (must match sender/receiver)
# These are the keyId and IV used by cloudflare-whip (sender) and whep (receiver)
DRM_KEY_ID=5ed8fa5fa9ae4f45fa981793a01f950c
DRM_IV=dc576fccde9d9e3a77cc5f438f50fd0f

# JWT Shared Secret (hex-encoded, must match DRMtoday dashboard)
DRM_JWT_SHARED_SECRET=f270c7854ea7278d1c68a68f6222993a175cf4357cdb07aa4da51669744def20424175a6f78dcd7a863611a223b337cd52754d3fb858ee025f5a97107d2f4dcc

# JWT Algorithm (HS256, HS384, or HS512) — working codebase uses HS512
DRM_JWT_ALGORITHM=HS512

# JWT Key ID (kid) — must match the kid configured in DRMtoday dashboard
DRM_JWT_KID=890c580d-4b51-4a71-b20e-5e126121bf4c

# JWT Token Expiry (in seconds)

DRM_JWT_TOKEN_EXPIRY=3153600000        # 10 years


# Authentication Configuration
# JWT Secret for user authentication session tokens (generate a secure random string)
AUTH_JWT_SECRET=my-super-secret-auth-jwt-key-for-development

# JWT Session Expiry (in seconds) for access tokens
AUTH_JWT_SESSION_EXPIRY=3153600000        # 10 years

# JWT Refresh Token Expiry (in seconds)
AUTH_JWT_REFRESH_EXPIRY=3153600000       # 10 years

# Cookie Secret for signed cookies (generate a secure random string)
COOKIE_SECRET=dev-cookie-secret-change-in-production

# Password hashing (bcrypt rounds)
BCRYPT_ROUNDS=12

# Default Asset ID (must match what DRMtoday has registered)
DEFAULT_ASSET_ID=test-key

# Default User ID for token generation
DEFAULT_USER_ID=elidev-test
 
# CORS Allowed Origins (comma-separated)
# IMPORTANT: Add your specific deployed frontend URL here
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174,https://localhost:8443,https://*.vercel.app,https://your-frontend.vercel.app,https://*.onrender.com

# Logging Level
LOG_LEVEL=debug
